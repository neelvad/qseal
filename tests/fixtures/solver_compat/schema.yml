tables:
  users:
    columns:
      user_id:
        nullable: false
      status: {}
    unique:
      - [user_id]
  dim_users:
    columns:
      user_id:
        nullable: false
    unique:
      - [user_id]
  fact_orders:
    columns:
      user_id:
        nullable: false
      revenue: {}
    foreign_keys:
      - columns: [user_id]
        ref_table: dim_users
        ref_columns: [user_id]
  orders:
    columns:
      user_id: {}
